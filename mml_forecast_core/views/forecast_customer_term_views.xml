<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_forecast_customer_term_list" model="ir.ui.view">
        <field name="name">forecast.customer.term.list</field>
        <field name="model">forecast.customer.term</field>
        <field name="arch" type="xml">
            <list editable="bottom">
                <field name="config_id"/>
                <field name="partner_id"/>
                <field name="rule_type"/>
                <field name="buffer_days"/>
                <field name="pay_day_of_month"/>
                <field name="notes"/>
            </list>
        </field>
    </record>

</odoo>
